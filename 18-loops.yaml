- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item }}"
      state: present
    loop:
    - { name: "nginx", state: "present" }
    - { name: "mysql", state: "abscent" }
    - { name: "zip", state: "present" }